#ifndef TEST_BLOCK_MODES_H
#define TEST_BLOCK_MODES_H

#include "testenv.h"

int test_block_modes(char *output, size_t maxlen, FILE *ext);
int test_block_modes_utilities(char *output, size_t maxlen, FILE *ext);

#endif
